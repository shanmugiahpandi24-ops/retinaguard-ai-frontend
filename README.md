# RetinaGuard AI — Frontend

A modern, production-quality React frontend built with Vite, TypeScript, and Tailwind CSS for the RetinaGuard AI FastAPI backend.

## Setup & Running Locally
1. Install dependencies: `npm install`
2. Configure `.env`: `cp .env.example .env`
3. Run dev server: `npm run dev`
